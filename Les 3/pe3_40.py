name = ["Gokmen","Marco", "Jan", "Zorro"]

def partition(namen):
    for names in namen:
        if names[0] < "N":
            print (names)
        else:
            print ("Niet in range")

partition(name)
